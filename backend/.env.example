# Supabase Connection
# IMPORTANT: Use port 6543 for Transaction mode (400+ connections)
# Port 5432 = Session mode (only ~10 connections - will exhaust quickly!)

DATABASE_URL="postgresql://postgres.waowoznsvaogsgcgiivzo:YOUR_PASSWORD@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=5"

# Direct URL for Prisma migrations (uses port 5432 on direct URL, NOT pooler)
DIRECT_URL="postgresql://postgres.waowoznsvaogsgcgiivzo:YOUR_PASSWORD@db.waowoznsvaogsgcgiivzo.supabase.co:5432/postgres"

# OpenAI API Key
OPENAI_API_KEY="your-openai-api-key"

# Server Configuration
PORT=8000
NODE_ENV=development
