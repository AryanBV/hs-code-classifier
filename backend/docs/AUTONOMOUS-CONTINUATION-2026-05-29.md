# Autonomous Continuation Brief — HS Code Classifier (2026-05-29)

Authoritative resume doc for the unattended run toward the "ultimate" ITC-HS 8-digit classifier.
Pair with memory: `project_vertex_m0_migration`, `feedback_calibrated_routing_not_restriction`, `feedback_root_cause_fix_not_patch`, `feedback_quality_first_best_in_class`.

## How to resume
- **Preferred:** continue the existing session — background-task completions re-invoke automatically; just keep driving the loop.
- **Fresh session:** read this file + `MEMORY.md`. Branch `feat/phase-4-pipeline-build`. All work is in the WORKING TREE (uncommitted by design — user commits when they return; do NOT push/commit unattended).
- Big analysis artifacts are session task outputs under the temp tasks dir: `w4q64m4jk`=M2 master plan, `ww6repvt0`=leaf-precision 9-cluster backlog, `w31tt5fhy`=QGS design, `wu1i3fwpu`=R1 regression diagnosis. (If the session is gone, re-derive via the workflows.)

## Post-auto-compact recovery protocol (user-mandated — do this FIRST after any compaction or fresh session)
STOP and regain 100% clarity BEFORE proceeding — do NOT guess or redo finished work:
1. Read this ENTIRE brief + `MEMORY.md` (esp. project_vertex_m0_migration, feedback_calibrated_routing_not_restriction, feedback_root_cause_fix_not_patch, feedback_quality_first_best_in_class).
2. `git status` + list `backend/eval-results/vertex-m0-*.json` — the highest round number is the current accuracy state; read its summary metrics.
3. `TaskList` for current progress; check which background workflow (if any) is mid-flight.
4. Reconcile: latest measured eval numbers + the change mid-flight + the next gated action (per the plan below).
5. Only once you can state the current accuracy numbers AND the next gated action with certainty, proceed autonomously.

## Operating rules (MUST follow — user-set)
1. **Quality-first, never reduce it.** "Right thing at the right time." Best-in-class over hand-rolled.
2. **Root-cause fixes, not patches.** No eval-overfitting — changes must be general/principled (no per-case or eval-ID hacks). A green eval via hacks = false pass.
3. **Measured gates, one change at a time.** Apply ONE principled change → full master eval → compare → keep ONLY if the **two-sided gate** holds: target metric up AND wrong-code rate flat/down. Never stack unmeasured changes.
4. **Orchestrate with dynamic workflows + as many agents as needed.** Use implement→review cycles + adversarial verification. After a delegated audit claims completeness, VERIFY yourself (grep) — agents miss sites (proven: re-audit missed the L5 cosine query).
5. **Calibrated routing (not restriction).** Classify when confident; ASK a *relevant number* of valid questions with *real options* (info-gain; not bombarding, not too few); abstain only for genuine junk. "Better no output than a wrong code, BUT don't over-restrict." Minimize wrong-codes AND needless asks/refuses together.
6. **Runtime = Vertex (credit-covered) — don't conserve Gemini/embeddings.** Cohere is OFF the v2 path; never reintroduce a Cohere dependency.
7. **Unattended limits:** no git commit/push, no deploy, no external data send. Park genuine user-only decisions here and keep working other items.
8. **Lean orchestrator — do NOT burn context.** Delegate bulk reading/analysis to agents; rely on their distilled structured returns + notification summaries; avoid reading large output files inline. Keep THIS brief updated at each milestone (it is the compaction lifeline).
9. **No questions until the user returns; no time pressure.** Proceed with best judgment on everything resolvable. Quality must INCREASE at every step, never drop. Keep going until ultimate is reached or a hard usage limit stops you. Don't artificially wrap up.

## Current state (2026-05-29)
- **M1 Vertex migration: DONE & verified.** Corpus 100% in `embedding_v2` (gemini-embedding-001@1536, all 4 levels + HNSW); supabase-client + L5 MV-04 query embedding_v2; Cohere off. Baseline `eval-results/vertex-m0-baseline.json`: routing 78.8%; on n=271 classify → chapter 93.7 / heading 90.0 / 8-digit 72.7 / weighted 86.3.
- **R1 routing calibration: DONE, KEPT.** L0 noise sanitization + L1 terse-specific ASK→CLASSIFY recalibration. Measured (`vertex-m0-r1`): over-ASK 51→26 (✅), over-REJECT 29→50 (regression), weighted 85.0. Diagnosis (`wu1i3fwpu`): R1 is CORRECT (15/16 lifts GT-aligned); regression = (a) L0 colon residue depressing L1 completeness (13 cases) + (b) pre-existing L4 over-refusal EXPOSED by routing more cases to L4 (22 cases).
- **R1-fix + R2 (L4 REFUSE): APPLIED + reviewed (575 tests, tsc clean).** L0 colon-separator strip (`/\s+:\s*|:\s+|:\s*$/gu`, preserves ISO:3234/1:2); select-v2.md REFUSE recalibration (reserve REFUSE for genuine unclassifiability: no-candidate OR clear different-chapter; soft predicate / pharma-dosage-form / terse-commodity ambiguity → best-fit + MEDIUM confidence, NOT refuse; genuine REFUSE preserved). Closing-reminder consistency fixed.
- **R2 (R1+L4 fix): MEASURED, KEPT.** `vertex-m0-r2`: routing **78.8→87.8%** (+9pp); over-ASK 51→28, over-REJECT 29→15 (both below baseline) — routing objective CLEARED. BUT recovering 37 borderline cases diluted classify accuracy (8-digit 72.7→**68.5%**, weighted 86.3→**83.1%**) and raised absolute wrong-codes ~+23. KEEP the routing recovery (don't re-refuse valid products); fix via PRECISION next, not reversion.
- **Chapter-confusion HS analysis: DONE** (w24v8equt). Root cause: L4 classifies parts-of-vehicles by MATERIAL not GIR-2(a) (parts solely-for-vehicle → that chapter). Verified-wrong (fixed): ceramic brake pads→87, nylon monofilament fishing line→54 (Ch.95 Note 1(c)), rubber oil seals/plastic dashboard→87. **GIR-2(a) parts fix DONE + reviewed** (w10myjupx, APPROVE_WITH_FIXES; select-v2.md Step 2a/2b, bounded by Section XVII Note 2; pump stays Ch.84 w/ test; 580/580 tests). **r3 KEPT** (chapter 91.2→93.5, heading 87→89.5, 8-digit 68.5→69.4, weighted 83.1→85.1, wrong-codes ~97→~90↓; routing 87.8→84.9 because model correctly REFUSES wrong material-chapter codes). Root cause of remaining refuses: L2 hard-filters retrieval to L1 candidate_chapters; parts queries omit host chapter (87) → 8708 never retrieved.
- **Retrieval host-candidate fix DONE + reviewed** (wzdv033xt, APPROVE_WITH_FIXES, 596 tests, tsc clean): bounded L1 post-step injects host chapter when PART head-noun + HOST keyword/intended_use co-occur (87 vehicles/86 rail/88 air/84 machinery; ≤1 chapter, capped at 3; reuses chapter-rules pattern). + maxOutputTokens 4096→8192 (kills MaxTokensError). general/bounded, overRetrieval LOW. **r4 KEPT** (routing 84.9→86.2, brake pads now→Ch.87; but accuracy diluted: 8-digit 69.4→68.7, weighted 85.1→83.5; oil-seals[ambiguous]/fishing-line persist). New MaxTokensError: S5-AMB-005 used 7861 thinking tokens even at 8192 budget (composite case — note for robustness workstream).

### STRATEGIC RE-ORIENTATION (2026-05-29, after r2-r4)
Routing rounds lifted routing 78.8→86.2% BUT headline accuracy drifted (8-digit 72.7→68.7, weighted 86.3→83.5) and est. wrong-codes rose ~74→~94 — mostly DILUTION (30 harder borderline cases recovered into classify) + LLM run-to-run NOISE (±1-2pp swamps small bounded fixes). Per user #1 ("better no output than wrong code"), routing-recovery has hit diminishing returns. **PIVOT: LEAF PRECISION** — close the heading(87%)→8-digit(69%) ~18pp gap (heading right, leaf wrong). This lifts correct AND cuts wrong codes, measured on the WHOLE pool (signal > noise). Then QGS for the genuine-ambiguity residual (ask, don't guess-wrong). Measurement lesson: for bounded fixes also check the TARGETED cases per-case (deterministic), not just noisy whole-suite deltas. **Dilution-vs-regression RESOLVED:** on 266 shared classify cases 8-digit HELD 72.2% (baseline 72.7%), heading flat 89.5% — the headline drop was DILUTION, NOT regression. Quality held on fixed cases. (3 minor reranker regressions DB058/077/192 — adjacent-chapter false positives — noted for later.)
**Leaf root cause:** of 56 heading-right-leaf-wrong, 37 (66%) have the discriminating attribute in tariff_line_attributes but L4 can't use it (gets 40+ raw JSON fields/candidate, NO sibling markers/comparison surface). 19 need NEW data (garment sizing, vehicle specs, surface treatment, fur species — separate data round). 0 retrieval misses.
**⏳ RUNNING NOW:** SIBLING-COMPARISON-VIEW fix — compute attributes that DIFFER across same-subheading sibling candidates + present the diff to L4 so the legal discriminator is explicit. General/principled (surfaces existing data). Targets 37 cases (~12pp 8-digit headroom). Verify whole-pool + per-case (TC013 car/truck tyres 4011.10 vs 4011.20, etc.).
**Sibling-comparison-view DONE + reviewed + fixed** (wb06gt6vx + review fixes: no-catch-all LOW-confidence guard, omit empty-diff groups; 610 tests, tsc clean). Diffs the 7 CORE attrs across same-subheading siblings → surfaced to L4. **LIMITATION (follow-up):** getTariffLineAttributesForCodes fetches only 7 core fields, so metadata-only discriminators (motor specs, fabric_construction) NOT yet covered — widen the fetcher next round for the ~14 metadata-discriminator cases. **r5 KEPT:** sibling-view works PER-CASE (TC013 truck-tyre now correct 4011.20.10) but whole-pool 8-digit 68.7→67.0 (noise + under-fueled: only 7/40 attr fields feed the diff). routing 87.0. TC305 wool-sweater hit 90s TIMEOUT (latency creeping — note). **⏳ RUNNING NOW:** COMPLETION round — widen getTariffLineAttributesForCodes to fetch metadata discriminator cols (motor specs, fabric_construction, chemical_class, predominant_element…) so the sibling-diff covers ~22 more cases; + diff r4↔r5 to confirm net-positive + check over-defer (sibling-view picking 6-digit/general too readily). Measurable beyond noise. **Fetcher-widening DONE + reviewed** (wcdm1jhcl, APPROVE_WITH_FIXES, 625 tests, tsc clean): +11 metadata discriminator cols (fabric_construction, chemical_class, predominant_element, in_solution, 5 metal %s, made_up, intended_role) feed the COMPACT sibling-diff; per-candidate block stays lean (token-disciplined, tested); Step 3a over-defer tightened (defer only when query genuinely silent); BONUS L5 can now resolve metadata predicates. Minor follow-up: add a boolean-metadata-discriminator unit test (no runtime effect). **r6 KEPT:** chapter 92.4/heading 88.4/8-digit 68.0/weighted 83.9, routing 86.5. 8-digit PLATEAUED ~68% across r4-r6 (noise band) — leaf thread at DATA CEILING (remaining = 19 data-gap cases + dilution; 0 retrieval misses). Per-case wins verified. **PIVOT to QGS thrust.**

### QGS THRUST (current focus, task #8)
8-digit can't pass ~68% via leaf tweaks (data ceiling). Two levers left: QGS (move hard borderline cases to ASK→answer→correct, instead of guess-wrong) + data enrichment (19 gap cases). QGS = user's explicit ask-path requirement + cuts wrong-codes + addresses dilution. BUT current eval can't measure QGS (asking a GT-classify case = routing miss). So:
- **STEP 1 (⏳ RUNNING):** build eval ANSWER-SIMULATION — for ASK outputs, derive the answer from the GOLD code's true tariff_line_attributes value for the asked discriminating attribute → continueWithAnswer (≤Q-budget) → score end-to-end. Add end-to-end-accuracy + ask-recoverability metrics; opt-in flag (`--simulate-answers`) so the existing routing metric is unchanged. NOT gaming — answers are gold-true values, measuring "if the user answers, do we get it right?".
- STEP 2: implement QGS multi-question (design tasks/w31tt5fhy.output; candidate-aware info-gain, now synergizes with the widened metadata fetch). Measure end-to-end before/after.
- LATER: data enrichment for the 19 gap cases (offline, O2-style).
- **Answer-sim DONE + reviewed + fixed** (w9qpsj0fv + symmetric substring-guard fix): eval runner `--simulate-answers` (default-off, existing metrics unchanged), derives answer from GOLD code's true attribute value → continueWithAnswer ≤Q-budget → end_to_end_accuracy + ask_recoverability_rate. Honest (no gold-code leakage, verified). Files: backend/src/eval/answer-simulator.ts, gold-attributes-lookup.ts, runner.ts, types.ts. **⏳ RUNNING NOW:** `vertex-m0-r6-sim` (answer-sim ON, current single-question ask-path) → end-to-end BASELINE before QGS. Then implement QGS (tasks/w31tt5fhy.output) → re-measure end-to-end lift.
**END-TO-END BASELINE (r6-sim):** ASK recoverability 41.4% (12/29), end-to-end 8-digit 66.7%, chapter 89.2/heading 85.5 (n=324). **QGS IMPLEMENTED + reviewed + fixed** (w7shf1rfd + Q-budget-rounds + observability fix; REJECTED reviewer's IG critique as mathematically wrong — balanced 2|2 split=1.0bit correctly > 3|1=0.811bit; IG left as-is). New QGS-generator.ts (info-gain greedy cap-3/floor-1), handleTriageAsk post-L3 candidate-aware + L1 fallback, batched continueWithAnswers (1 batch=1 round), answer-sim batch-aware. 702 tests, tsc clean. **r7-sim (QGS) MEASURED:** classify metrics FLAT vs r6 (8-digit 68.6, routing 86.4 — NO regression, gate passes). BUT ask_recoverability DROPPED to 25% (7/28) vs r6-sim 41.4% (12/29). **ROOT CAUSE = MEASUREMENT-HARNESS GAP, not QGS failure:** 16/28 ASK cases were "unanswerable" because `gold-attributes-lookup.ts` has no stored attribute matching the QGS-generated (candidate-aware) question. QGS is therefore UNMEASURED, not proven-bad. KEEP QGS (no main-path regression, it's the user's requirement); fix the harness coverage FIRST next session, then re-measure (r8-sim).

---
## SESSION-END HANDOFF (2026-05-29)
- **Ground truth:** branch feat/phase-4-pipeline-build, last commit 1593359, ALL work UNCOMMITTED (≈21 modified + new QGS/answer-sim files). tsc clean, **702 tests pass**. Latest PROD eval = r6 (routing 86.5/ch 92.4/hd 88.4/8d 68.0/wt 83.9). CLAUDE.md updated to current reality; this brief + MEMORY.md verified accurate.
- **Honest verdict:** M1 + capabilities (QGS, answer-sim, many verified fixes) are REAL; but headline 8-digit did NOT improve (72.7→68 = dilution; shared-266-subset HELD 72.2%), routing did (78.8→86.5). No net accuracy gain demonstrated yet. Inefficiency: serial 27-min evals on noise-dominated bounded fixes.
- **Scratch removed:** _commit_out.txt, tsc_out.txt, vitest_out.txt, scripts/_m0_result.json, _m0_embed_verify.ts, _diag_null_subheadings.ts.
- **Risks/incomplete (next session):** (1) UNCOMMITTED 9 rounds — COMMIT FIRST. (2) QGS recoverability unmeasured until answer-sim coverage fixed. (3) 8-digit ~68% = DATA CEILING (19 gap cases need offline O2-style attribute enrichment — biggest lever). (4) 3 reranker regressions DB058/077/192. (5) GT error TC009 (pump→84 is correct per Section XVII Note 2(e); GT wants 87). (6) eval denominator drifts — compare on the 266 shared-classify subset, NOT raw headline. (7) latency 20-44s/case + MaxTokens on composite cases. (8) cohere-client.ts may be dormant dead code — verify/remove.
- **BETTER PROCESS next session:** fast ~60-case eval subset (~5min) for iteration → full-386 only at milestones; per-case attribution (name the case IDs a fix targets + at-risk) before trusting deltas; run flag-off + flag-on evals in PARALLEL; commit at every kept gate; lean-orchestrator (delegate bulk, read only structured returns).
- **PRIORITY ORDER next session:** (1) commit + fix answer-sim coverage → real QGS gate (r8-sim); (2) DATA ENRICHMENT for the 19 gap cases (highest 8-digit headroom ~+5-6pp); (3) GT cleanup (TC009 + audit); (4) textile Ch.61/62/63 cluster; (5) L6/L7 only if verifier_rejected_but_correct proves need; (6) API rewire legacy→v2 + ship. Ultimate bars: chapter ≥95 / heading ≥88 / 8-digit ≥75-82 / routing ≥90 / weighted ≥87.
- Minor follow-ups (parked, do NOT block — citation-label precision only, no outcome effect): the "GIR 2(a)" label for the parts rule is technically Section XVII Note 3 / Section XVI Note 2(b) under GIR 1 (pre-existing in `backend/src/data/gir-rules.ts:47`); verify Section XVII Note 2 sub-letters (2(a)/(b)/(e)/(f)) against ITC-HS PDF in `backend/data/pdfs/` if MV-04 citation lookups ever fail on them.

### PARKED for user review (do NOT auto-fix during the run — possible eval GROUND-TRUTH errors / genuine ambiguity)
- **TC009 "fuel injection pump for car":** GT expects Ch.87 but Section XVII **Note 2(e) excludes pumps (8413) → Ch.84 is legally correct**. Our Ch.84 output is RIGHT; the GT is likely wrong. Recommend correcting GT after user review. (Do not chase — would be eval-gaming.)
- **Ambiguous (left alone):** silicone radiator hose (Note 2(a)/(b) material-exclusion vs vehicle-specific), some borderline rubber seals — defensible either way; candidates for the confusing-pairs / ASK path, not a forced rule.
- General: when later rounds find more suspected GT errors, LOG them here for the user; never silently rewrite ground truth to inflate the eval.

## Workstream plan (priority order — every item eval-gated)
1. **[in flight]** Confirm R1+L4 fix (`vertex-m0-r2`).
2. **QGS multi-question** (design READY in `w31tt5fhy`): move question-gen AFTER L3 (candidate-aware); info-gain over candidates' `tariff_line_attributes`; minimal sufficient set (cap 3, floor 1, stop when residual≤1 or marginal IG<0.25 bits); options from real attr values + other/none; extend `continueWithAnswer` to accept an answer batch. Implement→review→eval.
3. **Leaf precision** (8-digit 72.7%→≥75-82%). 9-cluster backlog in `ww6repvt0`: sibling indistinguishability (L4 attribute leverage), missing/misaligned tariff_line_attributes, FTS lexical gap (FIX-C synonym layer via O4 alias map `backend/data/build-time/O4-india-alias-map/aliases.json`), MV-04 near-sibling, GIR-3 composite, heading-level candidate collapse (FIX-D). Each measured.
4. **Chapter clusters:** vehicle-vs-component GIR-3 (Ch.87 precedence — brake pads/fuel pump/radiator hose currently →68/84/39), material-vs-use, textile siblings 61/62/63 (silk saree→62 not 50). L3 rules + L2 reweight + L4 prompt.
5. **Verifier calibration:** recalibrate MV-04 cosine floor for Vertex space (0.22 is Cohere-era — sweep against empirical Vertex distribution), MV-03 citation threshold, source-ref whole-notes defect, thread L4 context into L5 (avoid redundant DB fetches).
6. **Latency/perf:** ~20-44s/case vs p95≤8s; DB175 `MaxTokensError` (gemini-3.5-flash thinking-token budget) → raise maxOutputTokens; reduce repair-loop thrash.
7. **Reasoning layers L6 Tiebreak / L7 Deep-Think / L8 Active-Learning** — build ONLY if baseline proves need (verifier_rejected_but_correct rate, etc.).
8. **Trade intelligence (M4):** duty + export policy on every result.
9. **Productionization (M5):** API rewire legacy→v2 (`backend/src/api/classify.ts`), frontend wizard, PDF reports, CI, monitoring.

## Ultimate bars (measurable definition)
chapter ≥92→95%, heading ≥82→88%, **8-digit ≥75→82%**, weighted ≥87%, routing ≥90%, mean cost ≤$0.012/query, p95 latency ≤8s, verifier over-rejection ≤8%, ASK quality ≥95% targeted / ≥98% relevant, calibrated confidence, customs-officer-acceptable citations.

## Commands
- Eval (master, ~23 min, concurrency 8): `cd backend && npx tsx --require dotenv/config src/eval/runner.ts --suite master --run-id <id>` → `eval-results/<id>.json`. Quick suite: `--suite quick`.
- Tests: `cd backend && npx vitest run src/classifier-v2` (currently 575). Typecheck: `npx tsc --noEmit`.
- DB probes (read-only): `scripts/_check_progress.ts`, `_check_idx.ts`, `_inspect_embedding_cols.ts`.
- Layers: `backend/src/classifier-v2/layers/L0..L5`; prompts `backend/prompts/{triage,select}-v2.md`; lib: vertex-embed, embedding-provider, reranker, supabase-client, verifier-constants.

## Honest constraints
- **5-hour rolling usage limit** is shared across orchestrator + all subagents; high burn rate → expect a pause before "ultimate." Resumes after reset; this brief = lossless resume.
- "Ultimate" is multi-session. Target strong MEASURED progress per the gate, not completion-in-5h.
