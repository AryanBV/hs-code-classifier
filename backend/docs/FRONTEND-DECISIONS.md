# Frontend Decisions - completeness audit output

## Coverage summary

SOLIDLY PLANNED ALREADY (credit FRONTEND-PLAN.md + the frozen v2 DTO): The strategic spine is genuinely strong and largely settled. Locked and coherent: the FREE product with gate-artifacts-not-access (re-audit) and a guest-first soft gate; Supabase Auth (Google + magic-link); the value reframe to TOP-3 + a cited "rationale record" instead of a single-code oracle; honest COARSE confidence bands (drop %, never green-for-high); the three outcomes (CLASSIFY/ASK/REFUSE) at screen level; the Living-Certificate theme with Fraunces/Hanken/Commit-Mono fonts and light/warm-dark; the responsive PRINCIPLE (native desktop workspace + native mobile thumb-tool); hosting topology (Vercel + Render/Railway paid + Supabase Tokyo); brand (Prevyl @ hscode.prevyl.com); the compliance posture ("verify before filing"); abuse model (cookie-primary + Turnstile + rate-limits, explicitly NOT IP due to CGNAT); and DROPPING programmatic thin-page SEO. Crucially, the BACKEND has done more than the audits credit: the v2 DTO is ALREADY FROZEN in v2-api-adapter.ts as a flat discriminated union (classification|question|refused) with confidenceBand emitted, alternatives capped at 3 and EXCLUDING the primary (resolving two items the product-flow audit listed as "open"), plus citation/components/isSixDigit/exportPolicy/policyCondition/indiaSpecific. The /api/classify/answer route already enforces {originalQuery, questionId, answerId}. So the data contract the frontend must bind to is settled â€” the frontend simply hasn't been bound to it yet.

GENUINELY MISSING (verified in code): (1) The ENTIRE current frontend is a throwaway prototype on the OLD shape/value model â€” wrong brand ("TradeCode"), wrong fonts (Inter/JetBrains + a render-blocking Google-Fonts @import of IBM Plex/Space Mono), dark-default (plan locked light), a single-code oracle, a precise-% + green "High Confidence" emblem (hs-code-display.tsx lines 33-49 â€” the EXACT thing the re-audit killed), a hardcoded setTimeout liar-bar loader, alternatives hardcoded to [], no 'refused' screen state, and a /answer call that re-POSTs /api/classify with previousAnswers:{intended_use:label}. (2) App-shell surfaces don't exist: no not-found.tsx, error.tsx/global-error.tsx, sitemap.ts, robots.ts, manifest, icons, OG image. (3) Accessibility near-zero (infinite CSS animations not gated by prefers-reduced-motion; color-only confidence; no aria-live; sub-44px tap targets). (4) Desktop-native two-pane layout exists nowhere â€” everything is a centered max-w-lg column (the "blown-up mobile app on desktop" anti-pattern you rejected). (5) The concrete VISUAL spec for the result/rationale hero + loading + the signature first-impression moment is unwritten. (6) ASK and REFUSE screens lack concrete specs/copy. (7) Inline-sync-v1 reality conflicts with the SSE-assuming loader + "keep running" escape hatch. (8) Cross-cutting: no DPDP consent/deletion surface, no analytics/error-monitoring choice, no perf/CWV budget, no self-hosted-font wiring, env defaults stale (3001 vs backend 3000).

## Loopholes in decisions so far

- ACCESS-MODEL CONTRADICTION inside the plan: the master checklist still shows a LOCKED 'hybrid â€” ~3 free complete classifications then sign-up to continue' gate, while the authoritative re-audit says gate ARTIFACTS NOT ACCESS (unlimited free use; login only to save/export/sync). Both read as 'locked'; the re-audit wins per the precedence note, but the stale rows could be re-implemented by mistake â€” the contradiction already propagated into the cross-cutting audit.
- SSE-PLAN vs INLINE-SYNC-V1 REALITY: the locked 'honest, real-event-driven, NO timer' loader AND the '~30s keep-running, saved to History' escape hatch both ASSUME the SSE + classification_jobs backend, but v1 ships INLINE-SYNC (one HTTP request held up to ~80s, no SSE). Both have NO backing mechanism in v1; the plan sequences streaming AFTER the frontend yet the loader decision presumes it.
- TIMEOUT-vs-503 has no signal: the plan wants to reframe an 80s 'intricate' timeout differently from a transient 503 'hiccup', but the v2 adapter maps BOTH 80s-timeout AND system_error to HTTP 503 with no discriminator â€” the differentiation the copy promises is not expressible.
- CONFIDENCE DOUBLE-VIOLATION: current code (hs-code-display.tsx + confidence-meter.tsx) renders precise % + animated bar + green-for-high â€” the exact overclaiming the re-audit killed. The source is a 3-bucket SELF-rating mapped to 90/60/30 then a band; surfacing the 0-100 number re-introduces false precision the honesty principle forbids. The frozen DTO already carries confidenceBand â€” the UI must consume the band and HIDE the number.
- MULTI-TURN CONTRACT MISMATCH (silent data corruption): the frozen /answer contract is {originalQuery, questionId, answerId}; the hook re-POSTs /api/classify with previousAnswers:{intended_use:label} â€” a hardcoded attribute key and a label, not the question's id/answerId. Every answer is silently mismapped; multi-turn does not work against the real brain.
- ROUND-BUDGET INCONSISTENCY: pipeline truth = ASK up to 3 rounds; the hook uses MAX_QUESTIONS=5 / estimatedTotal default 4. The 'X of N' shown can contradict the real 3-round cap, eroding trust.
- GO-BACK DATA-LOSS TRAP: goBack() silently discards ALL prior answers and resets to input ('we can't easily revert API state'); the plan implies real sequential back-navigation â€” current behavior is hidden data loss.
- REFUSE UNREACHABLE: WizardScreen union is only input|loading|question|result|error â€” there is NO 'refused' state, so DTO responseType:'refused' falls through to a generic error or a broken result. A first-class decided screen is architecturally unreachable today.
- DARK-ONLY TOKENS BREAK LIGHT MODE: confidence-*, surface-*, ai-* CSS vars are defined ONLY under .dark; components using them in light mode silently fall back to dark HSL â€” 'light/dark parity' is structurally broken at the token layer.
- REDUCED-MOTION HALF-COVERAGE = false safety: Framer JS transitions honor useReducedMotion, but ~12 infinite CSS keyframe animations (blob/float/shimmer/glow/pulse) run unconditionally â€” a WCAG 2.3.3 vestibular failure despite the partial effort.
- HOVER-ONLY AFFORDANCE on touch: option cards express affordance via hover: only; on touch there is no hover and whileTap scale(0.995) is imperceptible â€” touch users get a flat, affordance-poor surface.
- FONT/THEME/BRAND DRIFT: code self-hosts none of the locked fonts (render-blocking Google @import + next/font load three font systems, none chosen), defaults to dark (plan = light), branded 'TradeCode' (locked = Prevyl) â€” the 'Living Certificate' identity is not what ships.
- ABUSE-MODEL HOLE: cookie-primary counting is trivially reset (clear cookies/incognito) and IP was deliberately rejected (CGNAT), leaving no robust second factor; the per-user free-count's relationship to the backend daily cost cap is undefined â€” a real cost-protection gap given the paid Gemini backend.
- ANALYTICS x DPDP COLLISION: any cookie-based analytics without a consent surface is a DPDP risk; neither is decided, and they interact.
- SEO FUNNEL WITHOUT AN ENGINE: the re-audit calls web a 'trust/SEO funnel' but drops the only stated acquisition mechanism (programmatic pages) without replacing it.

## USER DECISIONS NEEDED (count: 9)

### Decision 1: Confirm the current frontend/ is a THROWAWAY prototype to be REBUILT clean against the frozen v2 DTO + Customs-Ledger theme, not incrementally retrofitted. (Verified mismatches: wrong brand 'TradeCode', wrong fonts, dark-default, single-code oracle, precise-% + green emblem, setTimeout liar-bar, alternatives hardcoded [], no 'refused' state, wrong /answer contract.)
**Options:** A) Clean rebuild, keeping only reusable plumbing (next-themes, safe-area tokens, useReducedMotion transition pattern, Radix/shadcn primitives). B) Incremental retrofit of existing screens.

**Recommendation:** A â€” REBUILD. Almost nothing matches the locked plan or the frozen DTO; retrofitting risks smuggling the generic-AI oracle/percent/liar-bar aesthetic into the ship and costs more than a clean rebuild. The plan itself says the frontend rebuild MUST be planned with you first.

### Decision 2: Lock the per-viewport RESPONSIVE CONTRACT as a finite spec (your explicit #2 priority â€” currently only a slogan). For EACH core screen (input, loading, ASK, result/rationale, history): define the mobile-native form vs the desktop-native form, plus breakpoints.
**Options:** A) Mobile = single column + bottom-sheets + safe-area sticky primary CTA; Desktop (>=lg) = two-pane 'document left / living editorial margin right' (band, citations, export policy) + persistent history left-rail; breakpoints sm640/md768/lg1024/xl1280, two-pane engages at lg. B) Responsive-but-single-column everywhere (simpler, but is the 'blown-up mobile app on desktop' you rejected).

**Recommendation:** A â€” adopt the two-pane desktop workspace + mobile thumb-tool contract and ratify it as LOCKED before any build. I will draft per-screen annotated layouts; you approve the interaction model (e.g., does ASK use a full-screen sheet on mobile; does desktop render reasoning live in the margin). Can't be auto-defaulted without risking the exact anti-pattern you called out.

### Decision 3: Approve ONE signature first-impression 'wow' moment + a calm motion language for the Living-Certificate brand (your explicit #1 priority â€” inherently a taste/brand call). This reverses the current generic-AI maximalism (blobs/glow/float/shimmer).
**Options:** A) The finalized code 'stamps' into a certificate field with a subtle seal + the GIR/citation line writing in (ledger-stamp reveal). B) A quieter ledger-line/underwriting reveal. C) Another concept you prefer.

**Recommendation:** A â€” the ledger-stamp/seal reveal best embodies 'instrument that issues a living certificate'; adopt a calm motion budget (entrance-only 150-300ms eases, NO persistent ambient motion, all reduced-motion-gated) and kill blob/glow/shimmer/float from the design system. I'll mock 2-3 signature concepts visually for you to pick before build.

### Decision 4: Approve the REFUSE taxonomy + voice (liability-adjacent, brand-sensitive). Enumerate ~5-6 concrete refuse classes mapped from the brain's real refuse reasons, each with a calm non-blaming message + one concrete recovery action.
**Options:** A) Small fixed set (~5-6): out-of-scope / not-a-tradable-good / illegal-or-restricted / too-vague-even-after-asking / cannot-confidently-place â€” each with one next step (rephrase / add a detail / 'this isn't a tradable good'). B) A single generic 'we couldn't classify this' message.

**Recommendation:** A â€” define the small set and have you approve the copy/voice before build; the 'abstaining is a feature' honesty posture lives or dies on tone. I'll draft copy mapped to the actual out_of_scope_class enum; you sign off on voice.

### Decision 5: Decide the 6-digit vs 8-digit result presentation + inline 'verify before filing' treatment (a compliance-posture call you framed as legal honesty, not styling). Does isSixDigit change the hero / advisory line / PDF?
**Options:** A) When isSixDigit, present the 6-digit as the headline with an honest 'subheading-level; the final 8-digit needs verification for filing' note and surface candidate 8-digit leaves as the top-3; keep the advisory on both, stronger for 8-digit. B) Treat 6- and 8-digit identically.

**Recommendation:** A â€” it encodes the only compliant posture (unlicensed AI -> 6-digit; 8-digit 'for entry' is licensed customs business). Confirm so the hero + rationale record render the honest framing rather than implying filing-ready 8-digit.

### Decision 6: Approve the RATIONALE-RECORD field set (the moat artifact, on-screen + gated PDF). Which DTO fields appear in the certificate?
**Options:** A) query + top-3 codes&descriptions + chosen code + confidence BAND (no %) + GIR citation & verbatim note + plain-language reasoning + components (if GIR-3b) + export-policy/India-specific flags + generated-on date + advisory line. B) A leaner subset (code + reasoning + date).

**Recommendation:** A â€” include the full reasonable-care set; it IS the defensible artifact and the moat, and the DTO already carries every field. I'll draft the on-screen + PDF layout for your approval before build; you own the contents because it's a product+legal decision.

### Decision 7: Decide v1 LOADING UX + the '~30s keep-running, saved to History' escape hatch, given v1 is INLINE-SYNC with no SSE/job backend yet (a real sequencing conflict).
**Options:** A) v1 ships an HONEST single-hold staged loader (stage labels mapped to L0->L5 but NOT a fake timer) and NO escape hatch; defer the 'keep running / saved to History' affordance to the SSE+classification_jobs milestone. B) Pull the streaming/job backend forward so v1 has real progress events + the escape hatch.

**Recommendation:** A â€” ship the honest single-hold loader now (replacing the setTimeout liar-bar) and defer the escape hatch to the streaming milestone the plan already sequences after. Building the escape hatch on inline-sync is impossible; faking progress events re-introduces the liar-bar the plan kills.

### Decision 8: i18n scope for v1: English-only, or include Hindi/regional now? (Structural â€” retrofitting i18n after the rebuild is expensive; a market-positioning call about who the Indian-SME audience is.)
**Options:** A) English-only v1, but architect with next-intl from day one + pick fonts with the needed glyph coverage so Hindi is a later content-add, not a re-architecture. B) Ship Hindi (and which regional) at v1.

**Recommendation:** A â€” trade/customs vocabulary and HS descriptions are English-dominant, so English-only v1 is defensible; wiring next-intl now keeps Hindi cheap to add later. Confirm this is acceptable for your go-to-market.

### Decision 9: Shareable result permalinks + dynamic OG images: do classification records get a public URL (/r/:jobId) with a generated OG image, or stay private/client-only? (A growth-vs-confidentiality tradeoff that conflicts with your 'confidentiality as a feature' promise â€” only you can weigh it; it changes SSR/OG + classification_jobs RLS.)
**Options:** A) OPT-IN shareable permalinks: default private; user explicitly 'Share this record' -> mints a public, no-PII, OG-rendered page. B) Fully private/client-only (no permalinks, no virality lever). C) Public-by-default.

**Recommendation:** A â€” opt-in gives the SEO/virality lever the re-audit wants WITHOUT breaking the confidentiality positioning. Confirm so nuqs/jobId/OG and the RLS model are designed in, not retrofitted. (If you prefer to keep v1 dead-simple, B is the safe fallback.)


## Auto-handled (no decision needed)

- RETIRE the stale '~3-free-then-sign-up-to-continue' gate and implement gate-artifacts-not-access (unlimited free classify for guests; login only to save history / export PDF / sync) â€” the re-audit already wins per the precedence note; I'll strike the contradictory rows so it can't be re-implemented.
- DTO contract semantics are already RESOLVED by the frozen adapter (not open): top-3 = primary + up to 2 alternatives (alternatives EXCLUDES the primary, capped 3); confidenceBand is emitted (high|medium|low). I'll bind the frontend to the frozen v2 DTO exactly (flat discriminated union, all rich fields), fix the /answer call to {originalQuery, questionId, answerId}, add the missing 'refused' screen state, and remove the conversationId fabrication.
- CONFIDENCE UI honesty: consume confidenceBand only; HIDE the 0-100 number entirely; emblem = 'RULE-CHECKED Â· GIR Â· ITC-HS' + band; never green-for-high; band conveyed by text+icon+shape, not color alone. (Final band cutoffs/labels lock after backend ECE/Brier calibration; the UI is honest-by-construction regardless.)
- ASK screen pattern: single-question-at-a-time default (matches the brain's sequential repair); round indicator shown only as 'Question N' with NO fixed denominator unless a true QGS batch arrives (then '1 of N'); honest 3-round cap copy; always an explicit 'I'm not sure' option; real back-navigation that preserves prior answers (fixing the data-loss goBack and the MAX_QUESTIONS=5 vs 3-round mismatch).
- App-shell surfaces I'll add: app/not-found.tsx (404), app/error.tsx + app/global-error.tsx (route + global error boundaries), query-preserving retry. Timeout-vs-503: since the DTO can't distinguish them, use a single calm 'this one's intricate â€” try again' surface (split copy only if a discriminator is added); no fake distinction.
- Accessibility to WCAG 2.1 AA as a release gate: prefers-reduced-motion gating ALL infinite animations, >=4.5:1 verified contrast, aria-live stage announcements on loading, listbox keyboard nav for ASK options, focus-trap on sheets/modals, skip-link, 44px min touch targets, focus-visible rings; axe/Lighthouse a11y check in CI.
- Performance/CWV: self-host Fraunces/Hanken/Commit-Mono via next/font/local (subset + font-display:swap + preload), remove the render-blocking Google-Fonts @import and the double font-load, LazyMotion/code-split the wizard, set LCP/CLS/JS-bundle budgets, kill the paint-heavy animated-bg/glow effects.
- SEO basics (programmatic thin pages stay DROPPED): proper per-page metadata + title template, JSON-LD, sitemap.ts, robots.ts, a content-rich single product/marketing page, canonical + themeColor; fix brand metadata TradeCode->Prevyl.
- Branding: replace all 'TradeCode'/'HS Code Classifier' strings with the Prevyl + 'HS Code Finder' identity across layout/navbar/footer/metadata; set light as the default theme (plan-locked) with the warm-dark sibling toggle.
- Design-system foundation (engineering defaults, not taste forks): full Customs-Ledger warm-paper/ink color tokens with TRUE light+dark parity (fix the dark-only token leakage), spacing/radii/shadow scale with instrument restraint, a Fraunces-display/Hanken-body type scale, iconography set, core component inventory, and install the locked-but-missing libs (TanStack Query, RHF/Zod, Sonner, nuqs, Motion).
- Analytics + error monitoring with a privacy-first default: cookieless analytics (Plausible or PostHog cookieless mode) so NO cookie-consent banner is needed, + Sentry with PII scrubbing. (Flag for confirmation only if you want richer cookie-based analytics, which forces a banner.)
- DPDP-minimal compliance for a free pre-revenue MVP: vetted Privacy/Terms/Disclaimer (lawyer/template per plan) + the one-time 'I understand this is advisory' ack (cookie for guests, account flag for signed-in) + a self-service 'delete my data/history' action + named contact; defer a formal consent-manager. PWA manifest + installability + favicon/apple-icon as a cheap mobile-trust win.
- Env/CORS/security hygiene: add frontend .env.example, reconcile NEXT_PUBLIC_API_URL (3001->backend's real port), wire production CORS allowlist for hscode.prevyl.com, add Supabase publishable-key + Turnstile-site-key vars, baseline security headers/CSP.
- Empty/degraded result states: blank-description or empty-alternatives classification, slow-network 'still working' affordance, offline detection â€” handled with honest fallbacks (show what resolved, never fabricate).
- Feedback capture ('this code looks wrong', P0 in the backlog) wired as a lightweight on-result control that also feeds gold corrections.

## Readiness

READY to proceed after these 9 decisions. The hard external blocker â€” the v2 DTO â€” is NOT a blocker: it is already FROZEN in backend/src/api/v2-api-adapter.ts (flat discriminated union with confidenceBand, top-3 alternatives excluding the primary, citation/components/policy/India flags) and the /api/classify/answer route already enforces {originalQuery, questionId, answerId}. So the frontend can be designed and built against a settled contract today. Sequence: make decisions 1-9 -> I lock the design-system foundation + the per-viewport responsive contract + the result/rationale hero and signature reveal visually and present for sign-off -> then build the clean rebuild bound to the frozen DTO. TWO soft, non-blocking dependencies that do NOT gate design/build: (a) the final confidence BAND cutoffs/labels lock after the backend ECE/Brier calibration, but the UI is honest-by-construction (band-only, number hidden) regardless of where cutoffs land; (b) the SSE + classification_jobs streaming backend gates only the live-progress loader + the 'keep running, saved to History' escape hatch â€” v1 ships the honest single-hold loader (decision 7) and adds streaming in the milestone the plan already sequences after. Everything else is in auto_handled with senior defaults. Net: 9 genuinely-yours decisions, then design-then-build, no endless loop.

