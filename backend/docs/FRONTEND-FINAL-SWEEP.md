# Frontend Final Sweep — verdict

**VERDICT: settle-a-few-first**

## Summary
You are very close. The 9 locked decisions (D1-D9) are sound â€” I re-ran the adversarial steelman and found NO locked choice that a better option beats; the two 'reconsider' items are a one-line library-name fix (use `motion`, not `framer-motion`, for React 19) and a free container-query refinement to D2 â€” both I handle at build-time, neither needs you. Nearly everything the three sweeps surfaced is real but AUTO-FIXABLE by me without a decision: I will provision the missing tables (classifications, the classification_jobs table job-store already expects, feedback, shared_records â€” confirmed absent in the live Supabase schema, which today has only the 11 corpus/build tables), lock the auth UX, make band-only confidence structural (the DTO still ships the 90/60/30 number), strike the stale 3-free-gate rows, define the isSixDigit/empty-alternatives states, and settle the OG/permalink/offline/microcopy/alerting cluster with senior defaults. There are exactly THREE genuine decisions left for you, all true product/risk calls I should not make alone: (1) the cost-protection model â€” a GLOBAL hard daily ceiling in a shared store + Turnstile-on-classify is the only real financial backstop since per-user cookies reset and there is currently NO per-user enforcement at all (this is the one place a free tool on a paid backend can be attacked); (2) whether the COMPLETE rationale record is free on-screen (I recommend yes â€” gate only the PDF + permalink, which maximizes your impress-at-first-sight + shareability priorities); (3) single-question ASK at launch vs additively extending the frozen DTO for batched questions (I recommend single-question now â€” the adapter currently drops the batch anyway). Confidence: HIGH. Settle those three (each has a clear recommended default) and you are ready to design-then-build with no further loop â€” the frozen DTO and the v2 brain are genuinely a settled foundation.

## Left to decide

### Cost-protection / abuse model â€” the GLOBAL hard ceiling vs per-user friction (the single financially-dangerous gap on a FREE tool over a PAID Gemini backend).
**Why:** Verified in code: cost-monitor.ts enforces ONLY a global, in-memory, single-process daily ceiling (isOverDailyLimit) gated on MAX_CLASSIFICATIONS_PER_DAY; there is NO per-user/cookie/IP counter and NO Turnstile check anywhere in classify.ts. IP was rejected (CGNAT), so cookies are the only per-user signal and they reset on incognito/clear. Two real consequences you must accept or close: (1) a single abuser can hit the GLOBAL cap and 503 every user; (2) the in-memory counter resets on each Railway restart. This is the one place the product can be financially attacked and it is genuinely a product/risk call, not a default I can pick for you.
**Rec:** Lock this: (a) keep gate-artifacts-not-access (unlimited free classify) â€” correct; (b) add a SHARED-store global daily ceiling (move the counter into a Supabase row or Upstash so it survives restarts/replicas) as the true financial backstop that degrades to a calm 'we are at capacity today, try tomorrow' screen; (c) add invisible Cloudflare Turnstile as a server-side check on POST /api/classify so cookie-reset abuse costs a challenge solve; (d) a soft per-cookie/day count purely as friction, NOT an access gate. Accept that determined abuse is possible and the global cap is the ceiling. I need only your sign-off on the global daily number and that Turnstile-on-classify is acceptable.

### On-screen rationale record: gate the COMPLETE record behind login, or show it fully on-screen and gate only the portable artifact (PDF + permalink)?
**Why:** This is a genuine conversion-vs-moat-visibility tradeoff only you can weigh, and D6 itself flags the risk ('the gate must not fire before the value lands'). The moat IS the cited record; D9 leans on shareability as the acquisition engine. Gating the most screenshot-worthy thing throttles the exact word-of-mouth loop the funnel depends on. The DTO already carries every field for either choice, so it is purely a product decision with no backend cost.
**Rec:** For a FREE top-of-funnel product, show the COMPLETE on-screen rationale ungated for everyone (all expanders), and gate ONLY the formatted PDF download + the permalink mint. This maximizes first-impression moat and shareability (your priority #1) while keeping a real signup carrot (portable artifact). Confirm you are comfortable giving the full on-screen record away free.

### Does v2 ship single-question-at-a-time at launch (strike all '1 of N batch' ASK language), OR additively extend the frozen DTO to carry the QGS batch before it is truly frozen?
**Why:** Verified: the v2 pipeline CAN emit a ClarifyingQuestionBatch (types.ts: ClassifyResult.questions, 1..3 questions in one turn), but mapV2Result reads ONLY result.question (the first one) and ApiQuestionResponse carries no batch/count/total. So the auto-handled '1 of N when a true batch arrives' is unimplementable against the frozen DTO as written. A batch ASK is one turn = cheaper on the 5-RPM budget, so it is not purely cosmetic. This touches the word 'frozen', so you should decide before the wire is locked.
**Rec:** Ship single-question-at-a-time as the TRUE v1 contract and strike '1 of N batch' from the locked ASK pattern (pipeline still works; question mirrors questions[0], continueWithAnswer folds answers one at a time). Defer batch ASK to a fast-follow as an additive DTO field. Cleanest now; only flag if you specifically want batched questions at launch.


## Loopholes (fixes)

- **BLOCKER (auto-fixable, but needs build-zero provisioning): the entire persistence promise (history / save / sync / feedback / share / permalink) has NO table or write path. Live Supabase schema confirmed: only 11 corpus+build tables exist â€” NO classification_jobs, NO classifications/history, NO feedback, NO shared_records, NO user profile. job-store.ts queries classification_jobs which does not exist (the async path cannot run today). Current frontend history is localStorage-only with a syncedToBackend flag that never syncs.** -> fix: I will provision, as a build-zero step: (a) a `classifications` table keyed to auth user_id, owner-only RLS, storing the frozen DTO + query + timestamp; (b) the `classification_jobs` table the job-store already expects (CHECK-constrained status/kind, partial unique index on client_token, expires_at TTL) so the async/streaming path is flag-flippable; (c) a `feedback` table (classification ref + query + emitted code + optional suggested-correct-code + free text + cookie/user id + created_at); (d) a `shared_records` table (or is_public flag) with its OWN public-read RLS distinct from the private default, populated only on explicit share, carrying a PII-scrubbed snapshot. Guest = localStorage only; on first login, one-time MIGRATE localStorage history into the account. 'Don't save this' = skip BOTH server write and localStorage write. No user decision needed beyond the cost-model item above.
- **Auth UX is entirely unspecified despite being on the critical path for every gated artifact (save/export/share/sync).** -> fix: I will lock it as a senior default (no user decision): contextual bottom-sheet/modal at the gate (mobile-native) + header 'Sign in'; a dedicated /auth/callback route group; magic-link 'check your email' interstitial; @supabase/ssr cookie sessions with silent refresh; and a return-to-intent pattern (stash the pending action + result in sessionStorage, replay after auth) so 'click Export -> log in -> land back on that result' works. One coherent block, built before any gated control.
- **v1 inline-sync vs the already-built async job backend is internally contradictory and platform-risky: an 80s held request can exceed Vercel route maxDuration and proxy idle timeouts.** -> fix: Confirm v1 = inline-sync (correct) and I will plumb the timeout: call the Railway backend directly from the client with an ~85s fetch abort (avoid proxying the long hold through a Vercel serverless route, or set that route's maxDuration >= 85s) and verify Railway's proxy idle timeout exceeds 80s. The job-queue/job:id backend already exists behind CLASSIFY_ASYNC, so the async fallback is a flag-flip + the classification_jobs table I am provisioning â€” not a re-architecture. Single calm 503 retry stays (the adapter maps timeout AND system_error both to 503 with no discriminator â€” verified â€” so no fake distinction).
- **Stale '~3-free-then-sign-up' gate rows still read as LOCKED in FRONTEND-PLAN.md Â§2 (lines 47, 49) alongside gate-artifacts-not-access â€” a build session reading top-down could implement the wrong, unenforceable gate.** -> fix: I will physically strike / mark-SUPERSEDED the Â§2 '3-free hybrid gate' and 'free-count by IP + signed cookie' rows in FRONTEND-PLAN.md, pointing to gate-artifacts-not-access, so the contradiction cannot propagate into the build.
- **Confidence honesty is a UI convention, not structural: the frozen DTO ships confidence (90/60/30) AND selfConfidence alongside confidenceBand; one careless render re-violates the audited green-%-bar honesty fix.** -> fix: I will make band-only STRUCTURAL in the typed API client: map the raw response to an internal model that OMITS confidence and selfConfidence so no component can render the number (a type guard, not a guideline). Green-for-high banned in tokens; band conveyed by text+icon+shape. 'High' wording never implies certainty.
- **D4 refuse taxonomy: field-name conflation invites rendering raw internal prose. Verified: the adapter puts ClassifyResult.refusal.out_of_scope_class (the enum) into ApiRefusedResponse.reason and the model's free-text into message. The frontend MUST bucket on reason (enum) and MUST NOT display message. Also: D4's 'show closest chapters considered' has no DTO field to read from.** -> fix: I will lock the spec: bucket on the reason ENUM only (9 values + null -> the 5 buckets, null -> bucket 5 'couldn't confidently place'); NEVER render message to users (treat as debug-only). Refinement to the sweep: backtrack_no_fit and genuinely_indistinguishable ARE in the enum, so not every CLASSIFY-path refusal collapses to null â€” bucket 5 still needs honest copy without a sub-reason. 'Show closest chapters considered' is dropped for v1 unless you later want an additive consideredChapters[] DTO field (I will flag, not assume).
- **D5 isSixDigit branching has no defined behavior when alternatives[] is empty or description is blank â€” and the adapter legitimately produces both (never pads alternatives; leaf description falls back to '').** -> fix: I will define the empty states in the D6/D5 hero spec: isSixDigit=true AND alternatives empty -> headline the 6-digit subheading with the stronger advisory and an honest 'could not confidently enumerate the 8-digit leaves â€” a licensed CHA confirms the final digit' line, NOT an empty 'candidates' section; description='' -> render the code with its parent-chain/subheading-title fallback, never blank; 'never pad, never fabricate' as an explicit rendering rule. Goes through the same design sign-off.
- **'Confidentiality as a feature / don't-save-this' conflicts with auto-handled 'auto-save everything'; and once async/jobs are on, classification_jobs persists query text server-side with a TTL â€” so guest 'session-only' is not literally true on that path.** -> fix: I will resolve the default: signed-in = auto-save BUT a 'Don't save this' control visible AT INPUT TIME; guests = truly ephemeral (no server persistence of query text on the inline path). When async/jobs ship, guest job rows get a short TTL + no PII linkage, and Privacy discloses the job-row retention window. 'Don't save this' suppresses both history and job-row persistence. No user decision beyond the on-screen-record gating item.
- **Cookieless-analytics 'no consent banner' is undercut by (a) the cost-protection identity cookie and (b) D9 publishing user query text to a public indexable page.** -> fix: I will document the cookie inventory + legal basis: the counting cookie = strictly-necessary functional, httpOnly, disclosed as such, NEVER reused for analytics (preserves no-banner); analytics stays genuinely cookieless. For D9 share: the 'this will be public â€” shows your query and codes' confirm IS the consent â€” explicit, specific, logged with timestamp, with an inline edit of the displayed query (only the user knows if it is confidential) + a working self-serve takedown that purges the OG cache. A client-side warning fires if the query looks like it contains an email/phone/order-id before minting a public page.
- **OG image, /r/:id operational surface (rate-limit, takedown, indexability, no-PII enforcement), result permalink for the user's OWN record, copy-code/copy-record, first-run/empty state, edit-and-retry grammar, offline-mid-80s recovery, microcopy ownership, advisory-ack timing, monitoring/alerting â€” a cluster of medium/low items each with an obvious senior default.** -> fix: All auto-handled, no user decision: OG = next/og (Satori) at /r/:id/opengraph-image with the three fonts as buffers rendering a SIMPLIFIED flat-seal certificate, ISR-cached by record id, static fallback on throw. /r/:id = uuid (non-enumerable) + edge rate-limit + indexable + in sitemap (that IS the SEO lever) + takedown returns 410. Own record = private canonical route (/c/:id, owner-RLS) so refresh/bookmark survives. copy-code = bare code + toast; copy-record = plain-text twin of the PDF. First-run = input hero + 3-4 real example chips that prefill (no Gemini-costing demo) + returning-user collapse. Edit-and-retry = 'change last answer' re-submits previousAnswers minus subsequent; 'edit original query' = confirm-then-restart; trail kept client-side. Offline = typed query + answers in sessionStorage so a dropped 80s request returns to 'connection interrupted â€” your product is saved, try again', never blank. Microcopy = single next-intl en strings catalog + short voice guide, band labels/advisory/ack/error copy routed through the same sign-off as the refuse blocks. Advisory ack = non-blocking, persistent advisory line every result + one-time dismissible ack on first result (NOT a pre-input blocker). Alerting = Sentry/uptime alert at ~80% of MAX_CLASSIFICATIONS_PER_DAY + sustained-503 alert + /health uptime check to the founder email.

## Reconsider (better option beats current)

### D1 motion library (named in D3 as 'Motion (Framer) with LazyMotion' while the existing frontend imports framer-motion@12 across 10+ files).
- current: Ambiguous: 'Motion (Framer)' with salvaged framer-motion imports.
- better: Bind the spec to the `motion` package (`motion/react` + `LazyMotion`+`m`), forbid any salvaged `framer-motion` import, and document the shadcn `--legacy-peer-deps` install step (or use pnpm/bun).
- why: framer-motion is React-18-era and incompatible with React 19's concurrent rendering; only motion@^12 (the renamed package) is React-19-ready, and LazyMotion â€” the D3 CWV budget mechanism â€” lives in `motion`, not `framer-motion`. Any 'salvaged' framer-motion import breaks on the React 19 rebuild. This is a one-line lockfile decision, not a strategy change â€” the D1 stack itself is sound and production-solid as of mid-2026.
- rec: KEEP the D1 latest-stack decision; just bind motion+shadcn install specifics into the D1 'verify-and-lock the exact modern stack as the FIRST build step' guardrail. I will handle this during build-zero; no user decision.

### D2 two-pane switch trigger (locked as a viewport breakpoint at lg=1024).
- current: Page-level viewport media query engages the result card's two-pane split at lg=1024.
- better: Drive the RESULT CARD's document-vs-margin split off its OWN container width (@container, native in Tailwind v4); reserve the lg=1024 viewport breakpoint for the PAGE shell only (history rail present/absent, sticky CTA).
- why: A viewport-lg two-pane can fire while the actual main column is shrunk by an open history rail, cramping the unbounded verbatim citation â€” the viewport and the card's real available width disagree. Container-driven cards also render correctly in the history rail and the future embeddable widget (the re-audit names embeds first-class), paying off twice. The debate already identified this; it is a free refinement on Tailwind v4.
- rec: KEEP D2's dual-layout contract; adopt the container-query implementation note for the reusable cards. Auto-handled; no user decision.


