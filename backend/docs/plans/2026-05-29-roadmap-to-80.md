# Evidence-Based Roadmap to ~80% 8-digit (2026-05-29)

**Status:** ACTIVE plan-of-record for the capability climb (lever stack + method). Supersedes the P2 lever-ordering in `2026-05-29-ultimate-brain-program-v2.md` §0.5. **For live state + the "next" work order, canonical = `AUTONOMOUS-CONTINUATION-2026-05-29.md` §0/§6.** **Runtime = Vertex (Cohere DECOMMISSIONED).**

---

## SESSION-END STATE (2026-05-29 PM — current reality, READ FIRST)

- **HEAD `e340990`.** Honest baseline is now **r12 OUTRIGHT 8-digit 68.0%** (chapter 81.1 / heading 78.2), clean gold (32 GT corrections over 2 freeze rounds incl. **Gold-freeze Round 3** = 21 blind-verified user-approved corrections, commit `e48fdb7`), frozen routing-independent denominator (n=344). +5.8pp vs r9, McNemar p=0.0008 — the real win was fixing buggy gold; the brain was always ~68%.
- **gate-2 (this doc's §4 structural sibling fix: L-A sibling expansion + L-B discriminator-attrs into rerank + cap 8→12) = REGRESSED −2pp → REVERTED.** The wider/attributed candidate set confused Flash MORE, not less.
- **Pro-vs-Flash probe = DECISIVE:** Gemini-Pro ≈ Flash (fixed only ~6% of Flash's sibling errors; chose the SAME wrong sibling in 42/51). **The selection bottleneck is INFORMATION the query lacks, NOT the model and NOT complexity.** Adding candidates/attributes/reasoning/bigger-model does not help.
- **Therefore the Tier-1 "selection/sibling discrimination" thesis below is empirically de-prioritized for the live climb.** Half the residual "selection errors" were gold errors (fixed); the other half need an **ASK** (the deciding fact isn't in the query). The lead lever is now the **post-L4 uncertainty-gated sibling-ASK** (commit `e340990`, env-gated OFF, calibration r13 in flight).
- **LIVE "next" ORDER:** (1) **ASK-lever calibration** (r13 τ-sweep, three-sided gate) → (2) **retrieval-25% bucket** (Tier-2 below) → (3) **confidence calibration** (ECE ~18%) → (4) **ship** (API rewire → frontend → publish). Tier-1 selection levers are NOT the next move.

Baseline-of-record below (r9 62.2%) is retained for the diagnosis lineage; the current number is r12 68.0% above.

## 1. The measured diagnosis (why we were at 62%; now 68% after gold-freeze Round 3)

> Lineage note: this decomposition was computed at the r9 (62.2%) baseline. After Gold-freeze Round 3 (+21 corrections → r12 68.0%), roughly half of the "SELECTION" bucket below turned out to be GT-errors (brain was already right); the other half are genuine query-underspecification (→ ASK). The mechanism (sibling confusion / SELECTION-dominant) still holds directionally, but the *fix* is ASK + retrieval, not more selection context (gate-2 + Pro=Flash refuted that).

Error decomposition over 80 wrong gold-classify cases (replayed real L0→L3 + rerank), **corroborated by two independent papers** (arXiv 2605.14857 deterministic-HS-workflow: 22% recall vs 65% ranking error; arXiv 2509.18400 ATLAS: ~75% 8-digit cliff, "sibling confusion" = primary bottleneck):

| Bucket | Share | Meaning |
|---|---|---|
| **SELECTION** | **75%** (60/80) | gold leaf WAS in L4's ≤8 set, L4 picked a wrong sibling. **36/60 had gold in the reranker top-2.** 45/60 same 4-digit heading; 19/60 same 6-digit subheading. |
| RETRIEVAL | 25% (20/80) | gold never reached L4. ~half = L1 triage wrong chapter (TC009 pump→84; TC306 saree→50); ~half = genuine L2 leaf-recall. |
| RERANK-as-drop | 0% | reranker never dropped a retrieved gold (it's an ordering signal, folded into selection). |

**Honest ceiling (must internalize):** non-fine-tuned frontier+RAG systems report ~64–75% top-1 / ~78–91% top-3 at 6–8 digit. 62% is **in-band, not broken.** 80% top-1 without fine-tuning is the *optimistic edge*. **Realistic end-goal = ~75–80% top-1 via the levers below + a calibrated ASK path that converts genuinely-ambiguous siblings, with a fine-tuned domain reranker as the later true ceiling-raiser.** Report **top-1 AND top-3** as product metrics.

## 2. Root cause (mechanistic — from codebase analysis)

The 8-digit gap is structural, two coupled defects:
1. **No guaranteed sibling co-surfacing.** L2's cosine leaf query is `ORDER BY <=> q LIMIT 10` *global* across the 5 candidate subheadings (no per-subheading floor); siblings under the gold's subheading co-surface only *incidentally* (FTS luck or cosine slots). Once a leaf is in the union, **nothing pulls in its enumerated siblings** → L4's `computeSiblingDiscriminators` group often has <2 members → L4 picks blind among an incomplete sibling set. `L2-retrieval.ts` cosine cascade + `supabase-client.ts:317-344`.
2. **Reranker + L4 are blind to the distinguishing attributes.** RerankDocument = only `Chapter|Heading|Subheading|description`; near-identical siblings get near-identical scores; temp=0 ties them → tie-break = cosine order (not relevance). The 500-char cap clips the discriminating description *tail*. `reranker.ts:129-157`. (This is also why gate-1's L4-prompt-only fix was neutral — the inputs were incomplete/undifferentiated.)

## 3. Lever stack (evidence-ordered)

**TIER 1 — selection/sibling discrimination (attacks the 75%) — ⚠️ EMPIRICALLY DE-PRIORITIZED (2026-05-29):** L-A+L-B+cap were run as gate-2 and REGRESSED −2pp; Pro≈Flash. The bottleneck is INFORMATION the query lacks, not candidate completeness or model. These remain documented as a ceiling-raiser path (esp. L-G/L-I re-embed + fine-tuned reranker, which change the *information* the system has), but the live climb routes through ASK-calibration + retrieval-25% instead. Do NOT re-run L-A/L-B as-is expecting a selection win.**

_Original Tier-1 (kept for reference / future fine-tuned-reranker work):_
- **L-A Sibling expansion** (M, retrieval): after the L2 union, fetch ALL `tariff_line` children of the top-reranked leaves' subheadings and add missing siblings BEFORE rerank → guarantees the right leaf + true competitors reach L4 together. `L2-retrieval.ts:424-455` + reuse `getTariffLinesForSubheadings` (`supabase-client.ts:498`). ~~**Dominant fix.**~~ — hypothesized dominant; RAN as gate-2 and REGRESSED −2pp (see §4). Refuted as a selection lever.
- **L-B Discriminating attributes into rerank docs** (M, rerank): append a compact `tariff_line_attributes` discriminator line to each RerankDocument so the reranker can actually separate siblings. `L2-retrieval.ts:482-493`.
- **Hygiene** (S): rerank truncation keep the description tail not the chain prefix (`reranker.ts:129-139`); rerank `maxOutputTokens`→8192 + trace partial-degrade (`reranker.ts:273,286-302`); direct-leaf shortcut carry real subheading cosine score not 0 (`L2-retrieval.ts:446-455`); fix stale "1-5 candidates" → "1-8" in `select-v2.md:16,51`.
- **L-C Contrastive sibling-diff in L4-select** (S-M, selection): surface the exact distinguishing criteria per sibling group and require **POSITIVE description/attribute evidence** for the chosen leaf (FuDD method; the *corrected* gate-1 — positive-match, NOT elimination-license). Build after L-A/L-B land.
- **L-D Listwise + GIR-6 "most-specific-wins"** (S, selection): explicit comparative selection + notes_claims/GIR tie-breakers at sibling level.

**TIER 2 — retrieval recall (attacks the 25%):**
- **L-E Per-subheading leaf floor** in the cosine cascade (M): every retrieved subheading contributes its leaves (`supabase-client.ts:317-344`).
- **L-F RRF fusion + chapter-recall net** (M): reciprocal-rank-fusion dense+FTS (arms→30-40, fuse to 8); global cosine chapter probe / loosen backtrack so triage chapter-misses (TC009/TC306) are recoverable. `L2-retrieval.ts:424-455, 311-317`.

**TIER 3 — deeper / later:**
- **L-G Re-embed corpus with discriminating attributes** (L, retrieval): sibling vectors separate at cosine stage. Build-time Vertex re-embed. `reembed-corpus-vertex.ts:144-169`.
- **L-H Off-the-shelf cross-encoder** (BGE-reranker-v2-m3, self-hosted, M) as a stable deterministic pre-rank.
- **L-I (NOTE-FOR-LATER, gated):** fine-tuned domain embedding+reranker with hard-negative mining (siblings = hard negatives) — the true ceiling-raiser; needs training infra + labeled data. Start mining negatives now (free; the code trie defines them). RAG over Indian ITC-HS advance rulings (needs a ruling corpus).

## 4. Gate-2 — STRUCTURAL sibling fix (L-A + L-B + hygiene) — ❌ RUN → REGRESSED −2pp → REVERTED (2026-05-29)

**Disposition: REVERTED.** Bundled sibling expansion (L-A) + discriminator-attributes into rerank docs (L-B) + candidate-cap 8→12. Net effect: **−2pp 8-digit** — the wider, attribute-annotated candidate set made Flash MORE likely to pick a wrong sibling, not less. Combined with the Pro-vs-Flash probe (Pro≈Flash, same wrong sibling 42/51), this CONFIRMS the selection bottleneck is **INFORMATION the query lacks**, not candidate completeness, attribute annotation, or model capacity. Hygiene sub-items (truncation-tail, maxOutputTokens, direct-leaf cosine, "1-8" prompt text) are independently safe and may be cherry-picked if isolated, but the structural sibling fix as a selection-accuracy lever is refuted.

What was tried (kept for the record):
- **L-A** sibling expansion (bounded to subheadings of the top ~3-4 reranked leaves to cap prompt size).
- **L-B** discriminator-attribute line in RerankDocument.
- **Hygiene**: truncation-tail, maxOutputTokens, direct-leaf cosine, "1-8" prompt text.

**Live replacement for this slot:** post-L4 uncertainty-gated **sibling-ASK** (commit `e340990`, env-gated OFF; calibration r13 in flight) — converts the genuinely-underspecified siblings into one question instead of a blind guess. Blueprint: `2026-05-29-sibling-ask-lever-blueprint.md`.

## 5. Method (the pace fix)
- **Iterate on a TARGETED subset** via `runner.ts --ids <c1,c2,...>` = the current wrong-gold cases (recompute against **r12** clean gold — the original ~80-case list is stale post-freeze) + a correct-sample (regression guard), ~minutes — directly measures the target metric flip. **Full 386 only at a milestone** (a confirmed subset win). Compare runs with `compare.ts` (McNemar + three-sided gate).
- **Bundle coupled mechanism, then decompose** if mixed. Gate = selection-errors down AND no net regression on the correct-sample AND latency/cost in budget; McNemar at the full-eval milestone.
- Commit at every kept gate. Runtime stays Vertex.

## 6. Gate disposition log (2026-05-29)
- **gate-1** (L4 "Other"-by-elimination prompt): **NEUTRAL → REVERTED** (+0.3pp, McNemar p=1.0; the Step-3a elimination prompt actively mis-reasoned 0/2 when it fired — diagnosis `wax1dg5ig`). `isOtherLeaf` + Other-detection logic kept in `2026-05-29-gate1-other-elimination-blueprint.md`.
- **gate-2** (L-A sibling-expansion + L-B rerank-attrs + cap 8→12): **REGRESSED −2pp → REVERTED** (see §4).
- **Pro-vs-Flash probe**: **Gemini-Pro ≈ Flash** — fixed only ~6% of Flash's sibling errors, same wrong sibling 42/51. The selection bottleneck is **INFORMATION, not model and not complexity** (`pro-select-probe.json`).
- **Net:** three independent "improve selection by adding prompt/candidates/attributes/model" attempts all failed neutral-or-negative. The capability climb pivots to **ASK-calibration → retrieval-25% → confidence-calibration → ship** (see SESSION-END STATE).
